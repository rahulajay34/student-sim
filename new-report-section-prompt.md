# Task: Add an ADMIN-ONLY "New Report Section" to the counselling report (additive)

Repo: C:\Users\rahul\work\student-sim. Read CLAUDE.md + CONTRACT.md first.

## Goal (ADDITIVE — do not change the existing scoring/metric logic)
1. Keep every existing report section unchanged, but add a visible label "Old Report Section" above
   them (a divider/header before the "Rubric breakdown" card in ReportDetail.jsx).
2. Add a NEW admin-only "New Report Section" that scores the call on 8 parameters (each 0-5), shows
   each component's rating + a ONE-LINE AI summary personalised to that call, plus an overall score
   scaled to 100 (sum of 8 scores / 40 * 100). Layout mirrors the "Rubric breakdown" card.
3. The New Report Section is visible to admin/superadmin ONLY — never to the counsellor.

## CRITICAL architecture (verify before editing)
- DUAL backend. LIVE = Supabase Edge Functions (supabase/functions/*). Legacy Express server/* kept in
  parity. supabase/functions/_shared/lib/*.js are near-identical copies of server/*.js — make engine
  edits (report.js) in BOTH copies.
- Reports generated by supabase/functions/report-worker/index.ts -> generateReport() in
  _shared/lib/report.js. Persisted via commit_report RPC (supabase/migrations/0004_rpcs.sql). Reports
  table = hot columns + jsonb sections.
- RLS is row-level only: admin-only fields MUST be stripped at the edge for non-admins.

## The 8 parameters + EXACT 0-5 anchors (embed verbatim in the scoring prompt; 5 = "meets expectations", NOT superhuman; graders are strict, most scores are 2-4)
1. rapport_opening (Rapport & Opening)
   0 no greeting/rapport; abrupt; doesn't confirm who they're speaking to · 1 bare robotic greeting, no warmth ·
   2 greets+introduces self but generic, no agenda · 3 warm greeting+intro+basic agenda/audibility check, inconsistent ·
   4 warm personalised open (uses name), checks audibility/sets agenda, minor gaps · 5 excellent personalised warm opening, clear agenda, learner at ease
2. needs_discovery (Needs Discovery)
   0 no discovery; pitches immediately · 1 one/two generic questions, ignores answers · 2 some questions but pitches before understanding ·
   3 discovers background/goal but misses constraints (time/fee/family) · 4 uncovers background, goal & most constraints and tailors, minor gaps ·
   5 thorough discovery (background, goal, constraints, motivation) that clearly shapes the pitch
3. programme_presentation (Programme Presentation)
   0 no real presentation / wrong / confusing · 1 vague mention, no structure · 2 lists features but generic brochure-dump ·
   3 clear on curriculum/format/fees but partly generic/missing pieces · 4 structured, clear, mostly relevant, tied to learner, minor gaps ·
   5 crisp, structured, fully relevant, tailored to the learner's goal
4. objection_handling (Objection Handling)
   0 ignored/dismissed the concern · 1 acknowledged but barely addressed (no details) · 2 answered with no specifics (vague reassurance) ·
   3 answered partially but reasonably, some specifics, not fully resolved · 4 resolved most concerns with concrete specifics + empathy, minor gaps ·
   5 fully resolved each concern with specific facts/data/proof + workable alternatives; confident, no pressure
5. product_knowledge (Product Knowledge & Accuracy)
   0 major errors / fabricated / misselling (false guarantees) · 1 very shaky; doesn't know basics (fees/EMI/dates) or wrong info ·
   2 knows some facts but clear gaps/uncertainty · 3 mostly accurate, main facts, a few gaps/hedges · 4 strong accurate knowledge, minor gaps, no false claims ·
   5 complete, accurate, confident command of all details; no misselling
6. closing_payment_ask (Closing & Payment Ask)
   0 no close/next step · 1 vague "let me know", no ask · 2 mentions a next step but unclear/weak, no concrete ask ·
   3 asks for seat-block/next step but tentative/mistimed · 4 clear low-pressure ask, secures follow-up/near-decision, minor gaps ·
   5 confident, well-timed, low-pressure close securing a concrete decision or firm next step
7. communication_empathy (Communication & Empathy)
   0 confusing/rude/dismissive, talks over learner · 1 hard to follow, monologues, little empathy · 2 understandable but mechanical, limited empathy ·
   3 clear and polite, some empathy, occasional rambling/missed cues · 4 clear, structured, empathetic, responds well, minor lapses ·
   5 excellent: clear, warm, well-paced, genuinely empathetic and responsive throughout
8. personalised_experience (Personalised Experience)
   0 fully generic script, ignores who the learner is · 1 token nod but pitch is generic · 2 slight tailoring, mostly one-size-fits-all ·
   3 some genuine tailoring to background/goal, inconsistent · 4 pitch clearly mapped to this learner's background/goals, minor generic parts ·
   5 deeply personalised — every key point tied to this learner's profile, goals and constraints

## Backend
### A. New scoring call inside generateReport() — in BOTH _shared/lib/report.js AND server/report.js
Add buildNewReportPrompt(session) + NEW_REPORT_SCHEMA and run it as an independent call (mode:"reasoning",
jsonSchema-enforced) wrapped in its own try/catch (non-fatal, like the existing partial handling). Inputs:
- call context (Masai admissions counselling: seat-block ~Rs 4,000 + EMI; placement ASSISTANCE not
  guaranteed; certificate not degree; grade ONLY the counsellor),
- the learner for THIS call: session.personaSnapshot (name/label/coreAnxiety) and session.leadCard if present,
- the full session.transcript (role + text).
Ask the model to score each of the 8 parameters 0-5 using the EXACT anchors above AND write a ONE-LINE
summary of that aspect, specific to THIS call. Output schema:
  { "parameters": [ {"key": "<one of the 8 keys>", "score": <int 0-5>, "summary": "<<=1 sentence>"} (x8) ] }
Then in code attach: report.newReport = {
  total: round(sum(scores)/40*100, 1),
  parameters: [ {key, label, score, summary} for the 8 in fixed order ]
}
If the call fails -> leave report.newReport undefined (do not fail the whole report).

### B. Persist it
- New migration supabase/migrations/00NN_new_report.sql:
    alter table public.reports add column if not exists new_report jsonb;
    create or replace function public.commit_report(p_report uuid, p_token uuid, p_patch jsonb) ...
      -- add to the SET list:  new_report = coalesce(p_patch -> 'new_report', r.new_report)
      -- (keep the (uuid,uuid,jsonb) signature + existing grants; copy current body from 0004_rpcs.sql)
- supabase/functions/report-worker/index.ts buildCommitPatch(): if (report.newReport) patch.new_report = report.newReport;
- store.js report mapper in supabase/functions/_shared/store.js AND server/store.js:
    reportFromRow -> newReport: row.new_report ?? null
    reportToRow   -> if (obj.newReport !== undefined) r.new_report = obj.newReport;

### C. Admin-only exposure (the real boundary)
- supabase/functions/api/index.ts GET /reports/:id (~1061) and GET /reports (~1043): after the
  ownership check, if (ctx.role !== "admin" && ctx.role !== "superadmin") delete report.newReport (and
  delete from each item in the list). Mirror the same stripping in server/index.js (strip for a resolved
  non-admin counsellor; keep header-less back-compat-allow as existing guards do).

### D. Backfill — regenerate ALL existing reports so they get the new section
After A-C are deployed, regenerate every already-computed report so each one re-runs through the worker
and gains its newReport. There is an existing helper scripts/regen-all-reports.mjs — READ it first; if it
already re-kicks the report worker for every report, run it against the LIVE Supabase stack. Otherwise,
list all report ids and call POST /api/reports/:id/regenerate for each (admin/service auth), which resets
status to "generating" and re-fires the worker (api/index.ts ~1081). Throttle to avoid hammering the LLM.
Afterwards confirm every "ready" report row has a non-null new_report (a few will legitimately stay null
only if their session transcript is empty).

## Frontend — client/src/pages/shared/ReportDetail.jsx (+ a small new component)
- Broaden admin check: isAdmin = ["admin","superadmin"].includes(user?.role).
- Add a labelled divider/header "Old Report Section" immediately BEFORE the existing "Rubric breakdown"
  card (~line 535). Change nothing else about the existing cards.
- Add a NEW admin-only block, guarded { isAdmin && report.newReport && ( ... ) }, placed right after the
  HERO card (or just below the old sections — your call, keep it prominent). Title it "New Report Section"
  with subtitle "8-parameter evaluation · admin only". Inside:
    - show report.newReport.total as an overall (reuse ScoreMeter or the HeroPercent style),
    - render the 8 components like the rubric card: create a small presentational component
      NewParamBar({item}) (model it on src/pages/shared/RubricBar.jsx) that shows label, "score/5",
      a colored progress bar (width = score/5*100; color via scoreColor mapped from 0-5, e.g. <=2 danger,
      3 warn, >=4 success), and item.summary as the line beneath (the 1-line AI summary).
- Counsellors never receive newReport (stripped at the edge) AND the block is isAdmin-gated, so it is
  doubly hidden from them.

## The exact scoring prompt to embed (system + user)
SYSTEM: "You are a strict, experienced Masai mock-counselling grader. Score the COUNSELLOR on eight
categories using the exact 0-5 anchors. Be calibrated and strict (most real scores are 2-4; 5 = meets
expectations, not superhuman; 0 = absent). For each category also write ONE short sentence, specific to
THIS call, explaining the score. Return ONLY a JSON object."
USER: "<call context>\n<learner persona for this call>\n<the 8 parameters with their 0-5 anchors above>\n
Now read the full transcript and score each category 0-5 with a one-line summary.\n
=== TRANSCRIPT START ===\n<role:text lines>\n=== TRANSCRIPT END ===\n<schema>"

## Verification
- node scripts/check-edge-bindings.mjs (no missing imports); apply the migration to a branch DB and
  confirm reports.new_report exists and commit_report accepts the new key.
- End a session -> report-worker runs -> GET /reports/:id as ADMIN returns newReport {total, parameters[8]}
  each with a summary; GET /reports/:id as the owning COUNSELLOR -> newReport absent.
- UI: open /admin/reports/:id -> see "Old Report Section" label + a "New Report Section" with 8 bars +
  one-line summaries + overall/100. Open /app/reports/:id as counsellor -> only the old sections; no new
  section, no leak.
- After backfill (D), spot-check several PRE-EXISTING reports as admin and confirm they now show the New
  Report Section; a brand-new report without new_report still loads cleanly (block hidden).
- Reference the 8-parameter rubric/scoring built in C:\Users\rahul\work\gpt4o-latest\latest_scale.py
  (drop voice_delivery; /40*100) for exact wording.

Make engine edits in BOTH supabase/functions/_shared/lib/report.js and server/report.js. Keep changes
minimal and match existing patterns. Do not commit or push unless asked.
